# Lombok Configuration
# Copy @Qualifier annotations from fields to generated constructor parameters
# This ensures Spring can properly inject beans when using @RequiredArgsConstructor
lombok.copyableAnnotations += org.springframework.beans.factory.annotation.Qualifier
